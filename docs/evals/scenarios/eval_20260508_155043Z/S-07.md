# 场景评估：S-07

- **run_id**：eval_20260508_155043Z
- **用户输入**：我对花生过敏，请帮我记下来，以后推荐菜要避开。

## 期望摘要

偏好写入或显式确认流；Keeper 失败不挡主回复。

## 业务链路树（规格 §10.8.2）

用户输入：'我对花生过敏，请帮我记下来，以后推荐菜要避开。'
  ├── **意图识别** ✓ — router 完成且无 error_code
  ├── **槽位提取** — — —（steps 无 slots/missing_slots 摘要；可在 batch hints.business_chain_slots 注入）
  ├── **检索调用** — — 未触发 search_recipes
  ├── **后处理** — — 未触发 researcher/clarify_resolver/logistics 完成事件且无 get_recipe_source
  ├── **生成回复** ✓ — generator 完成且无 error_code

## 结论与失败锚点

（由评测方或后续 LLM-as-judge 填写；本文件由 CLI 自动生成骨架）

## steps[] 与效率摘要

- **steps 条数**：8
- **efficiency**：`{"tokens_input": 3130, "tokens_output": 108, "token_attribution": "round", "user_turns": 1, "graph_steps": 4, "latency_e2e_ms": 8679.198, "latency_node_p50_ms": 49.001, "latency_node_p95_ms": 8470.146, "redundant_mcp_search_count": 0, "redundant_mcp_get_recipe_source_count": 0, "redundant_call_count": 0}`

完整 `steps[]` 见同目录 `.json`。
