# 场景评估：S-02

- **run_id**：eval_20260508_155043Z
- **用户输入**：推荐一道低脂的家常菜。

## 期望摘要

完成检索或澄清；未索要则不强制展开库存与购物清单。

## 业务链路树（规格 §10.8.2）

用户输入：'推荐一道低脂的家常菜。'
  ├── **意图识别** ✓ — router 完成且无 error_code
  ├── **槽位提取** — — —（steps 无 slots/missing_slots 摘要；可在 batch hints.business_chain_slots 注入）
  ├── **检索调用** ✓ — search_recipes；query_used≈推荐一道低脂的家常菜。 [饮食约束] 不喜：芦笋 近期状态：肠胃不适 禁忌食材（须避开）：菌类；tool_ok=True
  ├── **后处理** ✓ — 已执行：researcher
  ├── **生成回复** ✓ — generator 完成且无 error_code

## 结论与失败锚点

（由评测方或后续 LLM-as-judge 填写；本文件由 CLI 自动生成骨架）

## steps[] 与效率摘要

- **steps 条数**：11
- **efficiency**：`{"tokens_input": 3125, "tokens_output": 93, "token_attribution": "round", "user_turns": 1, "graph_steps": 5, "latency_e2e_ms": 18877.23, "latency_node_p50_ms": 47.419, "latency_node_p95_ms": 9990.437, "redundant_mcp_search_count": 0, "redundant_mcp_get_recipe_source_count": 0, "redundant_call_count": 0}`

完整 `steps[]` 见同目录 `.json`。
